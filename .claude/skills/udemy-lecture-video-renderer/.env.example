# One-time per-user setup. Voice + API key are shared across ALL courses.
# The pronunciation dictionary is now auto-managed per-course — no env vars
# needed for it. See voice-clone-setup.md for how to populate these.

ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
